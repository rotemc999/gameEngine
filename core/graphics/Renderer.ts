namespace GE{

    const maxBatchQuads: number = 10000;
    //const maxTextures = gl.getParameter(gl.MAX_TEXTURE_IMAGE_UNITS);


    export class Renderer{
        private _batches: Batch[] = [];
        private constructor(){}

        public add(): void{
            
        }

        public render(): void{

        }
    }
}